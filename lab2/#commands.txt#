1) objdump -t bomb // смотрим, какие есть функции в программе. видим,
что есть phase_1, phase_2 и т.д.
2) gdb bomb // запускаем программу с помощью дебаггера
3) break phase_1 // устанавливам контрольную точку на первой функции
4) run // запускаем программу до первой контрольной точки
5) disas // делаем дизассемблинг
// видим сравнение на равенство
// видим вызов функции explode_bomb
6) stepi // делаем переходы внутри функции, доходим до подозрительной
// функции, сравнивающей строки
7) x /s $rsi // проверяем содержимое регистров,
// содержащих значения входных параметров
8) x /s $rdi
