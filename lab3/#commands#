break test     ; выставляем контрольную точку
run -u 1081119 ; запускаем с параметром
stepi 8        ; переходим к функции getbuf
stepi          ; заходим в функцию
stepi 4        ; доходим до функции Gets
x /80x $rsp    ; смотрим содержимое стека до
nexti  	       ; читаем строку функцией Gets
x /80x $rsp    ; смотрим содержимое стека после, видим, где зранится адрес возврата
